1. Run this command; pip install flask
2. source .venv/bin/activate       
3. Then run flask --app app run --debug 
4. Finally open the link in the terminal and begin to register!
5. For local database change end of link to "/registrants" if not taken there immediately
