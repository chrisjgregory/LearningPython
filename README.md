* Run this command; `pip install flask`
* `source .venv/bin/activate~~~~~~`
* Then run `flask --app app run --debug`
* Finally open the link in the terminal and begin to register!
* For local database change end of link to `/registrants` if not taken there immediately
